# Secure Finance RAG — 구현 및 운영 경계

이 프로젝트는 금융 사내 문서를 대상으로 하는 **검색 증강 생성(RAG) 포트폴리오 데모**다. 원본 파일은 사용자별 키 공간에 저장하고, 검색 가능한 데이터는 파싱된 청크와 임베딩으로 분리한다. 답변 단계에는 하이브리드 검색으로 추린 최대 3개 후보 청크만 전달한다.

| 파이프라인 | 데모 구현 | 운영 전환 시 권장 구성 | 평가 신호 |
|---|---|---|---|
| 1. 파싱·청킹 | Python XML/PDF 추출기와 문장 경계 기반 겹침 청킹 | 격리된 Python 워커, OCR, 표 구조 추출 | 텍스트 회수율, 청크 경계, 오류율 |
| 2. 임베딩 색인 | 결정론적 64차원 임베딩과 JSON 벡터 저장 | 사내 임베딩 모델과 사내 벡터 DB | 색인 커버리지, 중복률, 질의 재현율 |
| 3. 하이브리드 검색 | BM25 점수와 코사인 유사도 점수 가중 결합 | 금융 용어 사전·동의어 사전·재순위화 모델 | Recall@k, nDCG@k, 용어 재현율 |
| 4. 생성 | 후보 청크만 인용하는 근거 기반 데모 응답 | 사내망 로컬 LLM과 인용 검증기 | 근거 충실도, 인용 정확도, 완결성 |

## 점수와 개선 순서

각 단계의 점수는 0~100이며, 운영 목표 게이트는 **90점**으로 둔다. 개선 대상은 전체 파이프라인에서 단순 최저점이 아니라, 순서대로 확인했을 때 처음 90점에 미달한 단계다. 예를 들어 임베딩이 89점이고 검색이 78점이면 임베딩을 먼저 개선한다. 파싱·청킹과 임베딩의 품질 상한이 검색·생성의 입력 품질 상한을 결정하기 때문이다.

## 보안 경계

이 데모는 외부 검색엔진이나 외부 생성 모델로 문서 본문을 보내지 않는다. 실제 금융권 배포에서는 전용 네트워크 구간에 S3 Private Endpoint, KMS 암호화, 부서·문서등급 기반 접근제어, 감사 로그, 사내 벡터 DB, 사내 LLM 엔드포인트를 배치해야 한다. 현재 답변 엔진은 해당 경계를 검증하기 위한 후보-청크 한정의 결정론적 데모이며, 실운영 시에는 동일 인터페이스 뒤에 사내 로컬 LLM 어댑터를 연결한다.
