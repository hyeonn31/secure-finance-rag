# 로컬 마스킹 운영 가이드

이 도구는 **실제 금융 원본 데이터를 외부 AI나 외부 개발 환경에 올리지 않고**, 동일한 문서 구조·열 이름·데이터 유형·반복 관계를 가진 안전 샘플을 만들기 위한 로컬 변환기다. 프로그램은 네트워크 요청을 만들지 않으며, 입력 파일·마스킹 시크릿·원본과 토큰의 대응표를 출력하지 않는다.

> 마스킹은 보안 통제를 보완하는 기술이지, 단독으로 재식별 위험을 제거한다는 보증이 아니다. 실제 반출·모델 학습·검증 전에는 조직의 데이터 분류 기준, 정보보호 부서, 법무·준법 검토를 따라야 한다.

| 선택지 | 데이터 경계 | 생성 품질 기대치 | 주된 통제 | 적합한 상황 |
|---|---|---|---|---|
| 승인된 사내 AI | 사내망·승인된 클라우드 경계 | 중간~높음 | 접근권한, 감사로그, 승인 모델 | 운영 답변이 필요한 경우 |
| 로컬 모델 | 단일 PC 또는 폐쇄망 | 장비 사양에 의존 | 오프라인 실행, 디스크 암호화 | 최고 기밀·개별 분석 |
| 마스킹 샘플 + 외부 개발 | 원본은 사내, 샘플만 반출 가능 범위 | 개발·포맷 검증에 적합 | 토큰화, 최소화, 사전 검토 | UI·파서·프롬프트 형태를 개발하는 경우 |

## 보존하는 것과 제거하는 것

변환기는 CSV·JSON의 스키마, 행 수, 키 이름, 자료형, 동일 식별자의 반복 관계를 보존한다. 이메일·전화번호·주민등록번호 형태·카드번호·계좌번호·사업자번호 형태를 결정론적 토큰으로 바꾸며, `customer_name`·`account` 등 민감한 필드 이름의 값도 마스킹한다. 조직 고유 명칭은 `masking-config.example.json`처럼 로컬 매핑을 추가해 별칭으로 바꾼다.

변환기는 일반적인 정규식과 필드명 힌트를 사용하므로, 자유서술 텍스트의 모든 비정형 민감정보를 완전하게 탐지하지는 못한다. 따라서 산출 샘플은 반드시 사람 검토와 조직의 DLP 정책을 통과한 뒤에만 외부 환경에 사용할 수 있다.

## 사내 PC에서의 실행

Python 3.11 이상에서 원본과 스크립트를 **동일한 사내 PC 또는 승인된 사내 서버**에 둔다. `--salt`에는 조직이 관리하는 로컬 시크릿을 넣고, 해당 값은 외부 저장소에 커밋하지 않는다.

```bash
python3 scripts/local_masker.py \
  /secure/input/loan_review.csv \
  --output /secure/output/loan_review.masked.csv \
  --salt "LOCAL-ONLY-CHANGE-ME" \
  --config scripts/masking-config.example.json
```

같은 입력값과 같은 `--salt`는 같은 토큰을 만들므로, 고객·계좌·담당자 간의 반복 참조를 유지한 채 파서·청킹·색인·검색 흐름을 개발할 수 있다. 결과 옆에 생성되는 `*.mask-audit.json`은 마스킹 건수만 담으며 원본값·토큰 대응표·시크릿을 담지 않는다.

## PDF·DOCX·XLSX·PPTX 처리 원칙

문서 형식별 추출은 기존의 사내 파서로 먼저 수행하고, 추출한 텍스트·CSV·JSON 중간 산출물에 이 도구를 실행한다. 외부 환경에는 승인된 마스킹 샘플과 문서 포맷 정의만 전달한다. 실제 원본을 검색·임베딩·생성하는 운영 RAG는 승인된 사내 AI 또는 사내망 로컬 모델에서 수행한다.
